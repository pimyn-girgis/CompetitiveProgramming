#include <bits/stdc++.h>
using namespace std;

int k, q, m;
int main()
{
	while(scanf("%d%d%d", &k, &q, &m) != EOF)
	{
		if(k == q)
			printf("Illegal state\n");
		else
		{
			if(k/8 == q/8)
				{
					for(int i = 0; i < 8; ++i)
				}
		}
	}
	return 0;
}