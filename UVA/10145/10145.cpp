#include <bits/stdc++.h>
using namespace std;

int main()
{
	int tc;
	scanf("%d\n\n", &tc);

	while (tc--)
	{
		char mode;
		int trid, item;
		while(scanf("%c", &mode))
		{
			if(mode == '#')
				break;
			
			scanf("%d %d", &trid, &item);
		}
	}
	return 0;
}
